import { AudioProcessor, type IAudioProcessorContext } from 'agora-rte-extension';
declare class PcmAudioProcessor extends AudioProcessor {
    name: 'PcmAudioProcessor';
    private chunkProcessor?;
    private workletNode?;
    constructor(chunkProcessor: (data: ArrayBuffer) => void);
    protected onNode(node: AudioNode, context: IAudioProcessorContext): Promise<void>;
    startRecording(): void;
    stopRecording(): void;
    /**
     * en: Destroy and cleanup resources
     * zh: 销毁并清理资源
     */
    destroy(): void;
}
export default PcmAudioProcessor;
